var action_name : String

var tags_required_for_actor := []
var tags_required_for_subjects := []

func can_preform(actor : GameItem, subjects : Array[GameItem]) -> bool:
	
	for subject in subjects:
		pass
	
	return true

func preform(actor, subjects):
	pass
